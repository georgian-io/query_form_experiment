.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store

# Expensive caches (§9) and fetched corpora — regenerable, and they hold the dataset text
# we must not commit (ChatDoctor is real patient narrative).
# Anchored to the repo root: an unanchored `data/` also matches `src/data/`, which
# silently untracked every dataset adapter (source code, no dataset text).
/.cache/
/data/
