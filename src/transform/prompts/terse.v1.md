Rewrite the search below as a short query, the way a person would actually type it.

Keep only what determines which document answers it: the core information need, plus the
specific terms that constrain what counts as an answer — named entities, technical terms, and
any condition or qualifier a correct answer must satisfy. Drop narrative, background, and
restatement.

Write it the way someone searching would, not the way a document would. Do not invent details
that are not in the original.

{domain_hint}

Return only the query, with no preamble, quotation marks, or explanation.

Original:
{query}
