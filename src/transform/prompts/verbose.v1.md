Rewrite the search below as a full, natural-language information need — the way someone using an
AI assistant or a conversational search tool phrases it, rather than as keywords.

Expand it into complete, grammatical sentences that state the need explicitly. Make the implicit
context explicit, spell out what is being asked for, and use descriptive, specific language of the
kind that would appear in a document that answers it. Aim for a noticeably fuller phrasing —
typically several times longer than a bare keyword query (LLM-written search queries run to full
sentences, not a few keywords).

Do NOT change what counts as an answer. Preserve every answer-determining detail from the original
— named entities, technical terms, quantities, dates, conditions, qualifiers — and add NO new
constraint, requirement, fact, or assumption that is not present or directly implied in the
original. A document that answers the original must still answer your rewrite, and a document that
does not must still not. Elaborate the phrasing and the context, never the requirements.

{domain_hint}

Return only the rewritten query, with no preamble, quotation marks, or explanation.

Original:
{query}
